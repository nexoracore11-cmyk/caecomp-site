import Link from "next/link";
import { notFound } from "next/navigation";
import { getPublicData } from "../../lib/public-data";
const useful=["Calendário Acadêmico 2023.pdf","Fluxo Sugerido de Integralização.pdf","Fluxo Sugerido.pdf","Matriz Curricular.pdf","Portaria No 5011 de 28 de Agosto de 2025 - PFC.pdf","PPC EngComp.pdf","Resolução PFC - 2024.pdf","RGCG - Regulamento Geral dos Cursos de Graduação.pdf","Tutorial Matricula.pdf"];
const slugify=(v:string)=>v.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await getPublicData();const fixed=useful.find(n=>slugify(n)===slug);const item=data.content.find(i=>i.module==="documents"&&i.slug===slug&&i.documentUrl);const title=fixed||item?.title;const url=fixed?`/documents/${encodeURIComponent(fixed)}`:item?.documentUrl;if(!title||!url)notFound();const pdf=/\.pdf(?:$|\?)/i.test(url);return <><section className="document-reader-head"><div><span className="kicker">Leitor de documentos</span><h1>{title}</h1></div><div><Link className="button" href="/documentos">Voltar</Link><a className="button primary" href={url} download>Baixar PDF</a></div></section>{pdf?<section className="pdf-reader"><iframe src={`${url}#view=FitH`} title={`Leitor de ${title}`}/></section>:<section className="page-content"><p>Este formato não possui visualização no navegador.</p><a className="button primary" href={url} download>Baixar arquivo</a></section>}</>}
