"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
export type DocumentEntry={title:string;type:string;category:string;collection:"Documento"|"Jornal";url:string;slug:string;summary?:string};
export function DocumentLibrary({documents}:{documents:DocumentEntry[]}){const [query,setQuery]=useState("");const [type,setType]=useState("Todos");const [collection,setCollection]=useState("Todos");const types=["Todos",...Array.from(new Set(documents.map(d=>d.type)))];const filtered=useMemo(()=>documents.filter(d=>(type==="Todos"||d.type===type)&&(collection==="Todos"||d.collection===collection)&&`${d.title} ${d.type} ${d.category}`.toLocaleLowerCase("pt-BR").includes(query.toLocaleLowerCase("pt-BR"))),[documents,query,type,collection]);return <><div className="document-tools"><label><span>Pesquisar arquivos</span><input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nome, categoria ou tipo de arquivo"/></label><label><span>Área</span><select value={collection} onChange={e=>setCollection(e.target.value)}><option>Todos</option><option>Documento</option><option>Jornal</option></select></label><label><span>Tipo</span><select value={type} onChange={e=>setType(e.target.value)}>{types.map(t=><option key={t}>{t}</option>)}</select></label></div><div className="document-grid">{filtered.map(d=><article className="document-card" key={d.slug}><div><span>{d.collection} · {d.type} · {d.category}</span><h2>{d.title}</h2>{d.summary&&<p>{d.summary}</p>}</div><div><Link className="button" href={`/documentos/${d.slug}`}>Ler no site</Link><a className="text-link" href={d.url} download>Baixar arquivo</a></div></article>)}</div>{!filtered.length&&<div className="empty">Nenhum arquivo corresponde à pesquisa.</div>}</>}
