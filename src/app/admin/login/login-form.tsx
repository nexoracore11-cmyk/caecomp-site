"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(form:FormData){setBusy(true);setError("");const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password"),keepConnected:form.get("keepConnected")==="on"})});const data=await response.json();setBusy(false);if(!response.ok)return setError(data.error);router.replace("/admin");router.refresh();}return <form action={submit} className="login-card"><span className="kicker">Área restrita</span><h1>Painel CAECOMP</h1><p>Entre com a conta criada pela administração.</p><label>E-mail<input name="email" type="email" required autoComplete="email"/></label><label>Senha<input name="password" type="password" required minLength={8} autoComplete="current-password"/></label><label className="check"><input name="keepConnected" type="checkbox"/> Manter conectado por 30 dias</label>{error&&<div className="admin-error">{error}</div>}<button className="button primary" disabled={busy}>{busy?"Entrando...":"Entrar no painel"}</button></form>}
