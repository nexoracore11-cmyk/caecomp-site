import type { Metadata } from "next";
import { getPublicData, requirePublicSection } from "../lib/public-data";
import { EventBrowser } from "./event-browser";
export const metadata:Metadata={title:"Eventos"};
export default async function Page(){const data=await getPublicData();requirePublicSection(data,"events");const items=data.content.filter(i=>i.module==="events").sort((a,b)=>new Date(a.startAt||0).getTime()-new Date(b.startAt||0).getTime()).map(item=>({id:item.id,slug:item.slug,title:item.title,summary:item.summary,location:item.location,startAt:item.startAt,endAt:item.endAt,metadata:item.metadata}));return <><section className="page-hero"><span className="kicker light">Agenda CAECOMP</span><h1>Eventos que aproximam</h1><p>Workshops, palestras, visitas, recepções e encontros. Acompanhe vagas, lotes, mudanças e registros após cada evento.</p></section><section className="page-content">{items.length?<EventBrowser items={items}/>:<div className="empty">Nenhum evento publicado ainda.</div>}</section></>}
