import type { Metadata } from "next";
import { getPublicData, requirePublicSection } from "../lib/public-data";
import { CalendarBrowser } from "./calendar-browser";
export const metadata: Metadata = { title: "Calendário" };
export default async function Page() { const data=await getPublicData();requirePublicSection(data,"calendar");const items=data.calendarItems.map(item=>({id:item.id,title:item.title,summary:item.summary,startsAt:item.startsAt,endsAt:item.endsAt,kind:item.kind,department:item.department,sourceUrl:item.sourceUrl}));return <><section className="page-hero"><span className="kicker light">Agenda acadêmica</span><h1>Calendário</h1><p>Datas oficiais da UFG e atividades publicadas pelas diretorias autorizadas do CAECOMP.</p></section><section className="page-content calendar-page"><a className="text-link" href="/documents/Calendário%20Acadêmico%20UFG%202026.pdf" target="_blank">Abrir calendário acadêmico completo da UFG para 2026</a>{items.length?<CalendarBrowser items={items}/>:<div className="empty">O calendário será publicado em breve.</div>}</section></>; }
